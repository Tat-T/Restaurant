<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title</title>
    <link rel="stylesheet" href="/stylerest.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
</head>

<body>
    <div class="menu d-flex justify-content-between align-items-center p-2">
        <a class="nav-link ms-4" asp-page="Account/Login" data-bs-toggle="modal" data-bs-target="#registerModal2"><img
                src="/image/lk.png" width="50"></a>
        @if (User.Identity != null && User.Identity.IsAuthenticated && (User.IsInRole("Guest")))
        {
            <a class="nav-link fw-bold" asp-page="/Account/Index"><span class="p-2 fs-5">Профиль</span></a>
        }
        @if (User.Identity != null && User.Identity.IsAuthenticated && (User.IsInRole("Admin")))

        {
            <a class="nav-link fw-bold" asp-page="/Admin/AdminPanel"><span class="p-2 fs-5">Профиль</span></a>
        }
        <a href="tel:+89183586812" class="btn text-black fw-bold ms-auto me-4 fs-2">📞 8918-358-68-12</a>
        <ul class="navbar-nav d-flex flex-row position-absolute start-50 translate-middle-x fs-5">
            <li class="nav-item me-4">
                <a class="nav-link fw-bold" asp-page="/Index">Главная</a>
            </li>
            <li class="nav-item me-4">
                <a class="nav-link fw-bold" asp-page="/Menu">Меню</a>
            </li>
            @if (User.Identity?.IsAuthenticated ?? true)
        {
                <li class="nav-item me-4">
                <a class="nav-link fw-bold" asp-page="/Zakaz">Заказ столиков</a>
            </li>
            }
        @if (!User.Identity?.IsAuthenticated ?? true)
        {
            <li class="nav-item me-4">
                <a class="nav-link fw-bold" asp-page="/Zakaz" data-bs-toggle="modal"
                    data-bs-target="#registerModal">Заказ столиков</a>
            </li>
        }
            <li class="nav-item me-4 fw-bold">
                <a class="nav-link" asp-page="/Restaurant">Контакты</a>
            </li>
            <li class="nav-item me-4 fw-bold">
                <a class="nav-link" asp-page="/Spisok">Адреса сети ресторана</a>
            </li>
        </ul>
    </div>
    <div class="modal fade" id="registerModal" tabindex="-1" aria-labelledby="registerModalLabel" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="registerModalLabel">Регистрация</h5>
                    <button type="button" class="btn-close" style="box-shadow: none; outline: none;" data-bs-dismiss="modal" aria-label="Закрыть"></button>
                </div>
                <div class="modal-body text-center">
                    <p>Вы хотите зарегистрироваться или продолжить без регистрации?</p>
                </div>
                <div class="modal-footer">
                    <a href="/Account/Registration" class="btn btn-dark">Зарегистрироваться</a>
                    <a href="/Zakaz" class="btn btn-secondary">Продолжить без регистрации</a>
                </div>
            </div>
        </div>
    </div>

    <div class="modal fade" id="registerModal2" tabindex="-1" aria-labelledby="registerModalLabel2" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered">
            <div class="modal-content text-center">
                <div class="modal-header border-0">
                    <h5 class="modal-title w-100" id="registerModalLabel2">Вход / Регистрация</h5>
                    <button type="button" class="btn-close" style=" box-shadow: none;
    outline: none;" data-bs-dismiss="modal" aria-label="Закрыть"></button>
                </div>
                <div class="modal-body d-flex flex-column align-items-center justify-content-center">
                    <a href="/Account/Registration" class="btn btn-dark w-75 mb-3">Зарегистрироваться</a>
                    <a href="/Account/Login" class="btn btn-secondary w-75">Войти</a>
                </div>
            </div>
        </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    @RenderBody()
    @RenderSection("Scripts", required: false)

</body>

</html>
